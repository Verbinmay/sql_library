ТИПЫ ДАННЫХ
-- INTEGER
-- NUMBER(p,s) - p - количество цифр, s - количество цифр после запятой, то есть p - s это количество цифр до запятой

-- CHAR(length) - фиксированная длина строки (устарел, добавляет пробелы до длины)
-- VARCHAR2(length) - переменная длина строки

-- DATE - дата и время 
-- TIMESTAMP(f) - дата и время с точностью до f десятичных долей секунды

-- NULL 